spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userrepo
    username: root
    password: Chicago123@

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect